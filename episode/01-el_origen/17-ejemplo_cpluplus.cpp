#include <iostream>
#include <cstdlib>

using namespace std;

int main(){
  int numero;

  cout << "Ingrese un numero: ";
  cin >> numero; 

  if (numer>0) && (numero<100) {
    cout << numero << " está entre 0 y 100." << endl;
  } else {
    cout << numero << " está fuera del intervalo" << endl;
  }

  return 0;
}
